export const $=(id)=>document.getElementById(id);export const cleanCode=(v)=>v.toUpperCase().replace(/[^A-Z0-9]/g,'').slice(0,6);export const cleanNickname=(v)=>v.trim().replace(/\s+/g,' ').slice(0,18);export const validNickname=(v)=>/^[A-Za-z0-9 _-]{2,18}$/.test(v)&&!/https?|www\.|\.com/i.test(v);export const letters=['A','B','C','D'];
export function makeCode(){const chars='ABCDEFGHJKLMNPQRSTUVWXYZ23456789';return Array.from({length:6},()=>chars[Math.floor(Math.random()*chars.length)]).join('')}
export function makePlayerId(){const stored=sessionStorage.getItem('ihTriviaPlayerId');if(stored)return stored;const id=crypto.randomUUID().replaceAll('-','');sessionStorage.setItem('ihTriviaPlayerId',id);return id}
export function sortedPlayers(players={}){return Object.entries(players).map(([id,p])=>({id,...p})).sort((a,b)=>(b.score||0)-(a.score||0)||(b.correct||0)-(a.correct||0)||(a.joinedAt||0)-(b.joinedAt||0))}
export function scoreAnswer({correct,elapsedMs,durationMs}){if(!correct)return 0;const speedRatio=Math.max(0,Math.min(1,1-elapsedMs/Math.max(durationMs,1)));return 700+Math.round(300*speedRatio)}
export function confetti(count=70){if(window.matchMedia('(prefers-reduced-motion: reduce)').matches)return;const layer=document.createElement('div');layer.className='confetti-layer';const colors=['#e91e8b','#ffd337','#56a938','#713882','#f4812d','#0b8aa5'];for(let i=0;i<count;i++){const piece=document.createElement('span');piece.className='confetti-piece';piece.style.left=`${Math.random()*100}%`;piece.style.background=colors[i%colors.length];piece.style.animationDuration=`${2.4+Math.random()*2.2}s`;piece.style.animationDelay=`${Math.random()*.55}s`;piece.style.setProperty('--drift',`${-90+Math.random()*180}px`);layer.appendChild(piece)}document.body.appendChild(layer);setTimeout(()=>layer.remove(),5200)}
export function renderLeaderboard(tbody,players,currentPlayerId='',limit=10){tbody.innerHTML='';sortedPlayers(players).slice(0,limit).forEach((p,i)=>{const row=document.createElement('tr');if(p.id===currentPlayerId)row.className='you';row.innerHTML=`<td><span class="rank">${i+1}</span></td><td></td><td>${p.score||0}</td>`;row.children[1].textContent=`${p.nickname}${p.id===currentPlayerId?' — You':''}`;tbody.appendChild(row)})}
