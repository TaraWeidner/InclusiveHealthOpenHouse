import{firebaseConfig,firebaseConfigured}from'./firebase-config.js';const APP_KEY='ih-open-house-live-v1',listeners=new Map();let api=null,db=null;function readLocal(){try{return JSON.parse(localStorage.getItem(APP_KEY)||'{"rooms":{}}')}catch{return{rooms:{}}}}function writeLocal(data){localStorage.setItem(APP_KEY,JSON.stringify(data));window.dispatchEvent(new StorageEvent('storage',{key:APP_KEY,newValue:JSON.stringify(data)}))}function getAt(data,path){return path.split('/').filter(Boolean).reduce((v,k)=>v?.[k],data)}function setAt(data,path,value){const keys=path.split('/').filter(Boolean);let c=data;keys.slice(0,-1).forEach(k=>{c[k]||={};c=c[k]});c[keys.at(-1)]=value}function updateAt(data,path,updates){setAt(data,path,{...(getAt(data,path)||{}),...updates})}async function init(){if(!firebaseConfigured||api)return;const[{initializeApp},database,auth]=await Promise.all([import('https://www.gstatic.com/firebasejs/12.1.0/firebase-app.js'),import('https://www.gstatic.com/firebasejs/12.1.0/firebase-database.js'),import('https://www.gstatic.com/firebasejs/12.1.0/firebase-auth.js')]);const app=initializeApp(firebaseConfig);db=database.getDatabase(app);await auth.signInAnonymously(auth.getAuth(app));api={...database,...auth}}export const backendMode=firebaseConfigured?'live':'demo';export async function ready(){if(firebaseConfigured)await init()}export function serverNow(){return Date.now()}export async function getValue(path){if(firebaseConfigured){await init();return(await api.get(api.ref(db,path))).val()}return getAt(readLocal(),path)}export async function setValue(path,value){if(firebaseConfigured){await init();await api.set(api.ref(db,path),value);return}const data=readLocal();setAt(data,path,value);writeLocal(data)}export async function updateValue(path,updates){if(firebaseConfigured){await init();await api.update(api.ref(db,path),updates);return}const data=readLocal();updateAt(data,path,updates);writeLocal(data)}export function subscribe(path,callback){if(firebaseConfigured){let off=()=>{};init().then(()=>{off=api.onValue(api.ref(db,path),s=>callback(s.val()))});return()=>off()}const set=listeners.get(path)||new Set();set.add(callback);listeners.set(path,set);callback(getAt(readLocal(),path));const handler=e=>{if(e.key===APP_KEY)callback(getAt(readLocal(),path))};window.addEventListener('storage',handler);return()=>{set.delete(callback);window.removeEventListener('storage',handler)}}
